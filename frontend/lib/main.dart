import 'package:flutter/material.dart';
import 'const/navigation.dart';

void main() {
  runApp(
      const MaterialApp(
        debugShowCheckedModeBanner: false,
        home: Navigation(),
      )
  );
}
